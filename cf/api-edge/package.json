{
  "name": "opencomputer-api-edge",
  "version": "0.1.0",
  "private": true,
  "description": "OpenComputer api-edge Cloudflare Worker — Stripe webhook + /internal/halt-list (phase 2). WorkOS login + create proxy land in phase 3.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.0",
    "vitest": "2.1.9",
    "wrangler": "^3.80.0"
  },
  "dependencies": {
    "stripe": "^17.0.0"
  }
}
