name: Flue managed-build golden

on:
  push:
    branches: [main]
    paths:
      - 'cmd/oc/**'
      - 'ci/flue-builder-substrate-probe.py'
      - 'ci/test_flue_builder_substrate_probe.py'
      - 'ci/flue-managed-build-golden.sh'
      - 'deploy/flue-builder/**'
      - 'go.mod'
      - 'go.sum'
      - '.github/workflows/flue-managed-build-golden.yml'
  pull_request:
    paths:
      - 'cmd/oc/**'
      - 'ci/flue-builder-substrate-probe.py'
      - 'ci/test_flue_builder_substrate_probe.py'
      - 'ci/flue-managed-build-golden.sh'
      - 'deploy/flue-builder/**'
      - 'go.mod'
      - 'go.sum'
      - '.github/workflows/flue-managed-build-golden.yml'

permissions:
  contents: read

jobs:
  golden:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v4
        with:
          node-version: '22.19.0'

      - name: Verify pinned builder snapshot and substrate probe
        run: |
          python3 deploy/flue-builder/snapshot.py check
          python3 ci/flue-builder-substrate-probe.py --check
          python3 -m unittest ci/test_flue_builder_substrate_probe.py

      - name: Verify current CLI against pinned starter artifact
        run: ./ci/flue-managed-build-golden.sh
